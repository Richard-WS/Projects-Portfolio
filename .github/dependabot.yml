version: 2
updates:
  # Keep GitHub Actions up to date
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Python — add one entry per project once it has real dependencies,
  # e.g. directory: "/data/my-project"
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"

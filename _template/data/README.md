# Data

- `raw/` — never committed (gitignored). Downloaded or generated on setup.
- `samples/` — small, licence-clean samples may be committed for demos.

Document every data source and how to reproduce it in the project README.

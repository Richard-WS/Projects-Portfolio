# Shape-only example — this project needs no secrets.
# The pipeline downloads public Statistics Canada open data with no credentials.
# Copy to .env only if you ever need to override a path or URL; the defaults
# in configs/example.yaml work as-is.
CANLAB_DATA_DIR=data/raw
CANLAB_DB_PATH=data/canlab.db
